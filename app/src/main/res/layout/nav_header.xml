<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_header"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@color/md_grey_100"
    android:orientation="vertical"
    android:gravity="bottom"
    android:padding="@dimen/nav_normal_padding">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/civ_nav_header_avatar"
        android:layout_width="@dimen/nav_header_avatar_size"
        android:layout_height="@dimen/nav_header_avatar_size"
        android:src="@color/md_grey_100"
        app:civ_border_color="@color/md_grey_500"
        app:civ_border_width="1dp" />

    <TextView
        android:id="@+id/tv_nav_header_hint"
        android:layout_width="@dimen/nav_header_hint_width"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:textSize="@dimen/font_14"
        android:textColor="@color/md_grey_800"
        android:text="@string/nav_header_text" />

</LinearLayout>